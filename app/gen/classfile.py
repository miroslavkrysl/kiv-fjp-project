from app.gen.descriptor import MethodDescriptor
from app.gen.struct import Class

def _remap_collect_constants(cls: Class):
    constants: Dict[(str, MethodDescriptor), ]
    for method in cls.methods:
        for method in methods

def class_file(cls: Class):
